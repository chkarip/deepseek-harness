# Restrict root collection to the SDK tests and the runnable Python examples;
# recursive collection can include ignored worktrees or venvs and collide on
# same-named modules.
[pytest]
testpaths =
    python/sdk/tests
    examples/deepseek-skills/tests
norecursedirs = node_modules .git dist-exe
